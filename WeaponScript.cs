using UnityEngine;
using System.Collections;

public class WeaponScript : MonoBehaviour {
    public Transform WeaponTrans;
	// Use this for initialization
	void Start () {
     //   WeaponTrans.transform = transform.parent;
    }
	
	// Update is called once per frame
	void Update () {
      //  WeaponTrans.transform = transform.parent;
	}
}
